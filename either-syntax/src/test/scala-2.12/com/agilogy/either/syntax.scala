package com.agilogy.either

/**
  * For tests purposes only, so that the test can import com.agilogy.either.syntax in Scala 2.12
  * and run the same test suite against the standard lib Scala 2.12 Either
  * (so that we check the test is fine)
  */
object syntax
